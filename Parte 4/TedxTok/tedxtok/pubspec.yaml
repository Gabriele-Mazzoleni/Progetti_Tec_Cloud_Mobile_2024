name: tedxtok
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.2.10
  path_provider: ^2.0.6
  chewie: ^1.2.2  # Controlla la versione più recente stabile
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  # assets:
   # - assets/video.mp4
